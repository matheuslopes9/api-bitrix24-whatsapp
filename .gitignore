.env
CREDENCIAIS.md
bin/
sessions/
media/
*.db
*.log

# Artefatos gerados / config local / lixo de build
graphify-out/
.claude/
*.bak
*.exe

# Projeto de faturamento (contém certificados/chaves bancárias reais — NUNCA commitar)
faturamento/
# Exemplos de código do Itaú (referência local, não faz parte do app)
certificado-dinamico/
# Qualquer material sensível de certificado, onde quer que apareça
*.key
*.pfx
*.crt
*.pem
certs/

# Playwright MCP (screenshots/snapshots de teste)
.playwright-mcp/
welcome.png

# Serviço de e-mail (tools/oauth2-email-service): o CODIGO e versionado, as
# CREDENCIAIS nao. O .env traz client_secret do Azure AD da UC Technology.
tools/**/.env
tools/**/venv/
tools/**/__pycache__/
